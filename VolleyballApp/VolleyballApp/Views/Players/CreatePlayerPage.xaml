<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="VolleyballApp.Views.Players.CreatePlayerPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Name}" 
                   Placeholder="Имя игрока"/>
            <Entry Text="{Binding SquadNumber}" 
                   Placeholder="Игровой номер"/>
            <Picker Title="Позиция"
                    ItemsSource="{Binding Positions}"
                    ItemDisplayBinding="{Binding Path='Name'}"
                    SelectedItem="{Binding Position, Mode=OneWayToSource}"/>
            <Button Text="Назад" 
                    Command="{Binding PlayersViewModel.BackCommand}" 
                    CommandParameter="{Binding}"/>
            <Button Text="Добавить" 
                    Command="{Binding PlayersViewModel.AddPlayerCommand}" 
                    CommandParameter="{Binding}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>