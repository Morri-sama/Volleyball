<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VolleyballApp.Views.MainPageView">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Команды"
                    Command="{Binding TeamsCommand}"></Button>
            <Button Text="Игроки"
                    Command="{Binding TeamsCommand}"></Button>
            <Button Text="Матчи"
                    Command="{Binding TeamsCommand}"></Button>
            <Button Text="Команды"
                    Command="{Binding TeamsCommand}"></Button>
            <Button Text="Настройки"
                    Command="{Binding TeamsCommand}"></Button>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>